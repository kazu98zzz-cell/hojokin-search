# CONSTRAINTS.md — 不変条件・禁止事項（補助金検索ツール V1）

## 1. 絶対制約（違反=Red）
- CT-01: 公開サイト（public/配下）から外部API・外部ホストへ通信してはならない。読み込みは同梱の `data/subsidies.json` のみ。（NFR-COST-001）
- CT-02: 有料API・AI/LLM API・従量課金サービスを一切使わない。
- CT-03: jGrantsデータ利用時は出典表示（jGrants/デジタル庁＋取得日）を必須で表示する。（公共データ利用規約）

## 2. 外部アクセス制約
- CT-04: jGrants APIは取得スクリプト(fetch-data.js)からのみ。リクエスト間隔≥300ms・指数バックオフ必須（過負荷禁止）。
- CT-05: jGrants APIは読み取り専用GETのみ。書き込み/申請系エンドポイントは呼ばない。

## 3. セキュリティ制約
- CT-06: ユーザー入力・API文字列をDOMへ挿入する際は innerHTML直挿し禁止。textContent／エスケープを用いる（XSS対策）。
- CT-07: 個人情報・認証情報を扱わない・保存しない（ツールは匿名・状態レス）。

## 4. データ/運用制約
- CT-08: subsidies.json は取得スクリプトの出力物。手編集しない。
- CT-09: 既定は本番書換だが、動作確認は `--dry-run`/`--limit`/`--no-detail` を使う。

## 5. 依存制約
- CT-10: ランタイム依存ゼロ（Node標準のfetchのみ／npm install不要）。フロントはライブラリ非使用。

## 6. 失敗からの学習（FP）
- （運用中に発生した失敗を FP-XXX として追記する）
